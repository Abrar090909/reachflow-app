{
  "name": "reachflow",
  "version": "1.0.0",
  "description": "Self-hosted cold email outreach platform — a powerful alternative to Instantly.ai",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && nodemon server.js",
    "dev:frontend": "cd frontend && npm run dev",
    "install:all": "npm install && npm install --workspaces",
    "build:frontend": "cd frontend && npm install && npm run build",
    "build": "npm run build:frontend && mkdir -p dist && rm -rf dist/* && cp -r frontend/dist/* dist/",
    "start": "node backend/server.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.1.0"
  }
}